// Central app configuration flags
// USE_DEMO_AUTH: when true, components may use DemoAuthContext instead of real AuthContext
export const APP_CONFIG = {
	USE_DEMO_AUTH: false
};

export default APP_CONFIG;

